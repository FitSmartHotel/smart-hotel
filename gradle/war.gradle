apply plugin: "war"

bootWar {
    mainClassName = "com.smart.hotel.SmartHotelApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
